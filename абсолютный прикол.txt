#include <iostream>
#include <stdio.h>

using namespace std;

int main()
{
    setlocale(LC_CTYPE,"Russian");
    cout << "Работу выполнили сарычев сторожев авджян гр 219-1" << endl;
    cout << "Выберите (введите номер задачи) одну из трёх задач:" << endl;
    cout << "1. Подсчёт количества английских прописных и строчных букв в строке" << endl;
    cout << "2. Подсчёт количества слов в строке" << endl;
    cout << "3. Сравнение имени и его вывод в строке (своя задача)" << endl;
    cout << "Введите любое другое число для выхода из программы" << endl;

    start:

    int nomer;
    char arr[255];
    char hello[15] = "Здравствуйте, ";

    (cin >> nomer).get();
    cout << " " << endl;
    switch (nomer)
    {
    case (1):
        int b, s;
        b = 0;
        s = 0;

        cout << "Выполнение первой задачи" << endl;

        cout << "Введите строку символов: ";
        scanf("%s", arr);
        for (int i = 0; arr[i] != '\0'; i++)

            if ((arr[i] >= 'a') && (arr[i] <= 'z'))
                s++;
            else if ((arr[i] >= 'A') && (arr[i] <= 'Z'))
                b++;
        cout << "Количество строчных букв в строке = ";
        cout << s << endl;
        cout << "Количество прописных букв в строке = ";
        cout << b << endl;
        cout << " " << endl;
        cout << "Выберите задание из списка в начале" << endl;
        goto start;

    case (2):
        int counter, flag;
        counter = 0;
        flag = 0;
        cout << "Выполнение второй задачи" << endl;
        cout << "Введите предложение: ";
        gets_s(arr);
        for (int i = 0; arr[i] != '\0'; i++)

            if ((arr[i] != ' ') && (flag == 0))
            {
                flag = 1;
                counter++;
            }
            else if (arr[i] == ' ')
            {
                flag = 0;
            }
        cout << "Количество слов в строке = ";
        cout << counter << endl;
        cout << " " << endl;
        cout << "Выберите задание из списка в начале" << endl;
        goto start;

    case (3):
        cout << "Выполнение третьей задачи" << endl;
        int cmp;
        char name1[255];
        char name2[255];
        cout << "Введите ваше имя: ";
        gets_s(name1);
        cout << "Подтвердите  имя: ";
        gets_s(name2);
        cmp = strcmp(name1, name2);
        if ((cmp == 0) && (strlen(name1) == strlen(name2)))
        {
            strcat(hello, name1);
            cout << hello << endl;
        }
        else
        {
            cout << "Имена не совпадают" << endl;
        }
        cout << " " << endl;
        cout << "Выберите задание из списка в начале" << endl;
        goto start;
    case (0):
        break;

    default:
        cout << "Некоректный ввод, попробуйте снова" << endl;
        goto start;
    }
    return 0;
}